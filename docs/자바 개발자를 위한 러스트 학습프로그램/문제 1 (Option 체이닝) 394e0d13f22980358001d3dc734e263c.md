# 문제 1 (Option 체이닝)

## [문제 1] Option 체이닝과 기본값 처리

### 설명

자바의 `Optional`에서 사용하던 패턴을 Rust의 `Option` 메서드로 변환하는 훈련입니다.

사용자 정보가 담긴 구조체에서 보너스 점수를 계산하는 로직을 완성하세요.

- `score`가 있으면 `2`를 곱한 값을 반환합니다.
- `score`가 `None`이면 기본값 `0`을 반환합니다.

### 템플릿 코드

```rust
struct User {
    name: String,
    score: Option<i32>,
}

fn get_bonus_score(user: &User) -> i32 {
    // 이 부분을 구현하세요.
    // .map()과 .unwrap_or() 등을 체이닝하여 한 줄로 작성해 보세요.
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_option_chaining() {
        let user1 = User { name: "Alice".to_string(), score: Some(40) };
        let user2 = User { name: "Bob".to_string(), score: None };

        assert_eq!(get_bonus_score(&user1), 80);
        assert_eq!(get_bonus_score(&user2), 0);
    }
}
```