# 문제 3 (Fold를 이용한 리듀싱)

## [문제 3] fold를 활용한 데이터 축약 (Reducing)

### 설명

자바 스트림의 `reduce(initialValue, (accumulator, item) -> ...)`에 해당하는 Rust의 **`.fold()`** 메서드를 익히는 훈련입니다.

상품 정보가 담긴 구조체 리스트를 받아, **모든 상품 가격의 총합**을 계산하는 함수를 완성하세요.

- `.fold(0, |acc, item| ...)` 구조를 사용합니다. 첫 번째 인자는 초기값(seed)이며, 두 번째 인자는 누적 연산을 수행할 클로저입니다.

### 템플릿 코드

```rust
struct Product {
    name: String,
    price: u32,
}

fn calculate_total_price(products: &[Product]) -> u32 {
    // 이 부분을 구현하세요.
    // iter()와 fold()를 사용하여 총합을 구하세요.
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_iterator_fold() {
        let products = vec![
            Product { name: "Laptop".to_string(), price: 1000 },
            Product { name: "Mouse".to_string(), price: 50 },
            Product { name: "Keyboard".to_string(), price: 150 },
        ];

        let total = calculate_total_price(&products);
        assert_eq!(total, 1200);
    }
}
```