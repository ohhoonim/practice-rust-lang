# 문제 1 (String과 &str 변환)

## [문제 1] String과 &str의 상호 변환 및 가공

### 설명

자바의 `String`은 하나뿐이지만, Rust는 메모리 효율성을 위해 소유권이 있는 `String`과 참조용 문자열 슬라이스 `&str`로 나뉩니다.

성(Last Name)과 이름(First Name)을 참조자(`&str`)로 받아, 이를 대문자로 결합한 소유권 있는 고유한 `String` 풀네임을 반환하는 함수를 완성하세요.

- 두 슬라이스를 결합할 때 `format!` 매크로를 사용하거나 `to_uppercase()` 메서드를 활용하세요.

### 템플릿 코드

```rust
fn generate_uppercase_fullname(first: &str, last: &str) -> String {
    // 이 부분을 구현하세요.
    // 두 문자열을 합치고 대문자로 변환하여 소유권이 있는 String 구조체로 반환하세요.
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_string_handling() {
        let first_name = "john";
        let last_name = "doe";

        let fullname = generate_uppercase_fullname(first_name, last_name);
        assert_eq!(fullname, "JOHN DOE");
    }
}
```