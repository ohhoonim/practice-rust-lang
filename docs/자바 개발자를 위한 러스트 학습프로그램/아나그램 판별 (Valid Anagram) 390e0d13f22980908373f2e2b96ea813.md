# 아나그램 판별 (Valid Anagram)

- **목표:** 두 문자열이 서로 아나그램(문자의 구성과 빈도수가 동일한지)인지 판별하는 함수를 구현합니다.
- **조건:** `HashMap`의 `entry` API를 사용하여 문자별 빈도수를 효율적으로 카운팅하고 갱신하는 패턴을 익힙니다.

### [연습 문제] 아나그램 판별 (Valid Anagram)

주어진 두 문자열 `s`와 `t`가 아나그램이면 `true`, 아니면 `false`를 반환하는 `is_anagram` 함수를 완성하세요.

```
use std::collections::HashMap;

fn is_anagram(s: &str, t: &str) -> bool {
    // 이 부분을 구현하세요.
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_is_anagram() {
        assert_eq!(is_anagram("anagram", "nagaram"), true);
        assert_eq!(is_anagram("rat", "car"), false);
        assert_eq!(is_anagram("a", "ab"), false);
    }
}
```